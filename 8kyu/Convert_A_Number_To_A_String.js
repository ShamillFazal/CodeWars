// Convert a number to a string.

function numberToString(num) {

  // Use the 'toString' method to change 'num' to a string.

  let stringify = num.toString();

  // Return the converted string.
  
  return stringify;
}
