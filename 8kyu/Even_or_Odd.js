// Function to check if a number is even or odd.

function evenOrOdd(number) {
    // Check if the number is divisible by 2 (even).
    if (number % 2 === 0) {
      return "Even";  // Return "Even" if it's even.
    } else {
      return "Odd";   // Return "Odd" if it's odd.
    }
  }
  